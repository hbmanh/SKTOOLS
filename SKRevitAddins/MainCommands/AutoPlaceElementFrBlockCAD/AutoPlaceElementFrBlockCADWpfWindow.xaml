<Window x:Class="SKRevitAddins.AutoPlaceElementFrBlockCAD.PlaceElementsFromBlocksCadWpfWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Auto place element from block in CAD"
        Height="600" Width="980"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        ShowInTaskbar="True"
        Topmost="True">
    <Window.Resources>
        <Style x:Key="BlueHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#3399FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#3399FF"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo + tiêu đề -->
        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Image Source="pack://application:,,,/Resources/shinken.png"
           Height="30" Width="24"/>
            <TextBlock Text="Shinken Group®"
               FontSize="20" FontWeight="Bold"
               Margin="10,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Nút Enable/Disable All -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,8" Grid.Row="1">
            <Button Content="Enable All" Width="110" Margin="0,0,10,0" Click="EnableAll_Click"/>
            <Button Content="Disable All" Width="110" Click="DisableAll_Click"/>
        </StackPanel>

        <!-- DataGrid trong Border -->
        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Margin="0,0,0,0" Grid.Row="2">
            <DataGrid x:Name="BlockMappingGrid"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding BlockMappings}"
                      RowHeight="30"
                      FontSize="13"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      Height="420"
                      MinWidth="900"
                      AlternatingRowBackground="#FFF2F2F2"
                      AlternationCount="2"
                      ColumnHeaderStyle="{StaticResource BlueHeaderStyle}">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Enable" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Block Name" Binding="{Binding DisplayBlockName}" Width="*" MinWidth="120"/>
                    <DataGridTextColumn Header="Count" Binding="{Binding BlockCount}" Width="70" MinWidth="60" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Category" Width="160" MinWidth="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Path=CategoriesMapping}"
                                          DisplayMemberPath="Name"
                                          SelectedItem="{Binding Path=SelectedCategoryMapping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="True"
                                          StaysOpenOnEdit="True"
                                          IsTextSearchEnabled="True"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Family Name" Width="220" MinWidth="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Path=FamiliesMapping}"
                                          DisplayMemberPath="Name"
                                          SelectedItem="{Binding Path=SelectedFamilyMapping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="True"
                                          StaysOpenOnEdit="True"
                                          IsTextSearchEnabled="True"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Type Name" Width="200" MinWidth="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding Path=TypeSymbolsMapping}"
                                          DisplayMemberPath="Name"
                                          SelectedItem="{Binding Path=SelectedTypeSymbolMapping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsEditable="True"
                                          StaysOpenOnEdit="True"
                                          IsTextSearchEnabled="True"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Offset (mm)" Binding="{Binding Offset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" MinWidth="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Nút OK/Cancel -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="3" Margin="0,12,0,0">
            <Button Click="OkBtn_Click"
                    Name="OkBtn"
                    Margin="25,5,5,5"
                    Content="OK"
                    Width="100"
                    MinHeight="32"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Background="#3399FF"
                    Foreground="White"/>
            <Button Click="CancelBtn_Click"
                    Name="CancelBtn"
                    Margin="35,5,5,5"
                    Content="Cancel"
                    Width="100"
                    MinHeight="32"
                    FontWeight="Bold"
                    HorizontalAlignment="Center"
                    Background="WhiteSmoke"/>
        </StackPanel>
    </Grid>
</Window>
